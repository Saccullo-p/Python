Struttura dati = Una struttura dati è un'entità usata per organizzare un insieme di dati all'interno della memoria del computer.
Lista = Una lista è una struttura dati astratta e dinamica che denota una collezione di dati.
List comprehension[] = La list comprehension è una tecnica procedurale per creare liste partendo da un'espressione, all'interno della stessa riga di codice.
Tupla = Una tupla è una sequenza immutabile di elementi eterogenei che ammette duplicati.
Set = I set sono strutture dati simili alle liste con la funzionalità di capire se una lista o un dizionario contiene un determinato valore.
Dizionario = Un dizionario è un oggetto contenente coppie di "chiave-valore".
Funzioni = Le funzioni sono strumenti che permettono di raggruppare un insieme di istruzioni da eseguire, accettando parametri in input, elaborandoli e restituendo un risultato.
Parametri formali = I parametri formali sono le variabili all'interno delle definizioni delle funzioni.
Parametri attuali = I parametri attuali sono i dati reali che sostituiscono i parametri formali, svolgendo le istruzioni.
Struttura di controllo = Una struttura di controllo è un insieme di istruzioni che controllano il flusso di codice.
